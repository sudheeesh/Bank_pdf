const { generateAmountsForExistingRows } = require('./engine/generateTransactions'); const txs = Array.from({length: 120}).map((_, i) => ({date: '01/' + String(Math.floor(i/10)+1).padStart(2,'0') + '/2025', desc: 'TEST', debit: 0, credit: 0})); const res = generateAmountsForExistingRows(txs, {openingBalance: 50000, closingBalance: 600000, maxMonthlyDebit: 28000, maxMonthlyCredit: 10000, maxTxnDebit: 3000, maxTxnCredit: 3000}); console.log('bad diffs:', res.filter(t => t.credit > 3000 || t.debit > 3000));
